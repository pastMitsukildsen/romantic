<div class="heading">
<h1 class="major">Sign up Form </h1>
</div>

<div class="sign-up">
  <h1 class="sign-up-title">Sign up in seconds</h1>
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="field">
    <%= f.text_field :name, autofocus: true, autocomplete: "name", class: "sign-up-input", placeholder: "What's your username?" %>
    </div>

    <div class="field">
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "sign-up-input", placeholder: "Email address" %>
    </div>

    <div class="field">
      <% if @minimum_password_length %>
      <em>(<%= @minimum_password_length %> characters minimum)</em>
      <% end %><br />
      <%= f.password_field :password, autocomplete: "new-password", class: "sign-up-input", placeholder: "Password(6 characters minimum)" %>
    </div>

    <div class="field">
      <%= f.label :password_confirmation %><br />
      <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "sign-up-input", placeholder: "Password confirmation" %>
    </div>

    <div class="actions">
      <%= f.submit "Sign up", class: "sign-up-button" %>
    </div>
  <% end %>
  <div class="legal-text">
    続行することで、Romanticの
    <%= link_to "利用規約", "#" %>
    および
    <%= link_to "プライバシー規約", "#" %>
    に同意するものとみなされます。
  </div>
  <div class="box-newusr">
      <p><span>-------------</span> 既に登録済みの方 <span>-------------</span></p>
      <%= link_to "ログインページ", new_user_session_path %>
  </div>
</div>

